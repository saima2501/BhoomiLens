# Kept minimal — every extra megabyte lives forever in the ECR image.
boto3>=1.34,<2.0

# PaddleOCR + its runtime. Hindi and English models are downloaded to /tmp
# on cold start.
paddleocr==2.7.3
paddlepaddle==2.6.2

# PDF → image conversion. pdf2image needs poppler installed via the
# base image's dnf, which we do in the Dockerfile.
pdf2image==1.17.0

# Image utilities
Pillow>=10.0

# Optional: better rotation/deskew for scanned docs
opencv-python-headless>=4.9
numpy>=1.26,<2.0
